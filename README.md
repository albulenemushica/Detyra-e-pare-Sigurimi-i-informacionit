# Detyra-e-pare-Sigurimi-i-informacionit,

Programimi me C# i  ByteSub (nonlinear layer) /ShiftRow (linear mixing layer)  / MixColumn (nonlinear layer) / AddRoundKey (key addition layer) tek Algorimi AES	
